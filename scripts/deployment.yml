# Write Kubernetes Deployment Script

apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-catalog-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: book-catalog
  template:
    metadata:
      labels:
        app: book-catalog
    spec:
      containers:
      - name: book-catalog
        image: book-catalog-microservice:local
        ports:
        - containerPort: 5000
        env:
        - name: DATABASE_URL
          value: "postgresql://rauan_sit722_unillibrary_user:1TCilpWHcagmDQAlW8d4j4XG4zXCajuy@dpg-cr85k03qf0us73bq5n80-a/rauan_sit722_unillibrary"